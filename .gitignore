# Dependencies
node_modules/

# Build-Output
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env / Secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.local

# Netlify
.netlify/

# Lokale Backend-Laufzeitdaten (Interaktions-Zustand + Einstellungen mit API-Key!).
# Nur die JSON-Daten ignorieren — der geteilte Backend-CODE (server/core.ts) wird
# committet und deployt (von vite.config.ts und der Netlify Function importiert).
server/*.json
