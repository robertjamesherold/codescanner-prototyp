import useActiveRoute from './useActiveRoute';
import useSideBarOpen from './useSideBarOpen';

const hooks = {
  useActiveRoute: useActiveRoute,
  useSideBarOpen: useSideBarOpen,
};

export default hooks;
